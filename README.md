# NostrImgUploader

Upload file so that you can use them on Nostr

## Getting started

- clone this repo
`git clone`

- install the deps 
`yarn`

- setup env variable
fill host on VITE_HOST field

- launch React
`yarn dev`